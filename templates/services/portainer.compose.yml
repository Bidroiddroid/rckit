services:
  portainer:
    image: portainer/portainer-ce:2.21.5
    restart: unless-stopped
    ports: ["127.0.0.1:${PORTAINER_PORT:-9443}:9443"]
    volumes:
      - /var/run/docker.sock:/var/run/docker.sock
      - portainer-data:/data
volumes:
  portainer-data:
