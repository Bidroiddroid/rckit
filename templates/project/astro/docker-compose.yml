services:
  app:
    image: node:22
    working_dir: /workspace
    volumes:
      - .:/workspace
    ports:
      - "127.0.0.1:4321:4321"
    command: ["npm", "run", "dev", "--", "--host", "0.0.0.0"]
